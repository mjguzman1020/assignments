# Lab 4: Child Vaccination Chart

# Main dictionary to hold patient data
patient = {}
```
# Function to insert new patient
def insert(first_name, last_name, birth_month):
    # Create a unique record number
    record_number = len(patient) + 1
    
    # Create a nested dictionary for the patient
    patient[record_number] = {
        "First Name": first_name.title(),
        "Last Name": last_name.title(),
        "Vaccine Month": birth_month
    }

# ---------------- Demonstration ----------------
print("=== Child Vaccination Chart ===")

while True:
    # Get user input
    f_name = input("Enter patient's first name: ")
    l_name = input("Enter patient's last name: ")
    b_month = int(input("Enter vaccine month (1-12): "))

    # Insert into dictionary
    insert(f_name, l_name, b_month)

    # Show dictionary after insertion
    print("\nCurrent Records:", patient)

    # Ask if user wants to continue
    choice = input("Add another patient? (Y/N): ").strip().upper()
    if choice != "Y":
        break

print("\nFinal Patient Records:", patient)
```
